# Porta em que o NGO service recebe requisições
PORT="8081"

# URL authenticada do DB
DATABASE_URL="postgres://sol:sol@psql:5432/sol_db"

# Desativa o SDK OpenTelemetry no ambiente local - o tracing (Tempo) é exclusivo do cluster
OTEL_SDK_DISABLED="true"