apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# AWS Load Balancer Controller: pré-requisito do modelo de endpoint único do
# EKS (ver lb-controller/README.md e kube-aws/README.md). Reconciliado antes
# de kube-aws/ - ver o dependsOn em clusters/eks-aws/solidarytech-kustomization.yaml.

resources:
  - 000-serviceaccount.yaml
  - 010-helmrepository.yaml
  - 020-helmrelease.yaml
